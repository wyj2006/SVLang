pub mod parser;

use crate::parser::{Rule, SVParser};
use pest::Parser;

fn main() {
    // let a = SVParser::parse(Rule::source_text, "module a(); endmodule").unwrap();
    // println!("{:#?}", a);
}
